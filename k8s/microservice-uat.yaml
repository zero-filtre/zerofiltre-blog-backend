apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: zerofiltretech-uat
  name: zerofiltretech-blog-uat
spec:
  minReadySeconds: 30
  progressDeadlineSeconds: 120
  replicas: 1
  selector:
    matchLabels:
      app: zerofiltretech-blog-uat
  template:
    metadata:
      labels:
        app: zerofiltretech-blog-uat
    spec:
      containers:
        - name: zerofiltretech-blog-uat
          image: imzerofiltre/zerofiltretech-blog:0.0.1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  namespace: zerofiltretech-uat
  name: zerofiltretech-blog-uat
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: zerofiltretech-blog-uat
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  namespace: zerofiltretech-uat
  name: zerofiltretech-blog-uat
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    # add an annotation indicating the issuer to use.
    cert-manager.io/cluster-issuer: letsencrypt-uat
spec:
  rules:
    - host: "blog-api-uat.zerofiltre.tech"
      http:
        paths:
          - path: /
            backend:
              serviceName: zerofiltretech-blog-uat
              servicePort: 80
  tls: # < placing a host in the TLS config will indicate a certificate should be created
    - hosts:
        - "blog-api-uat.zerofiltre.tech"
      secretName: zerofiltre-ingress-cert # < cert-manager will store the created certificate in this secret.