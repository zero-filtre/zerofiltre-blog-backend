logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
server:
  port: 80

springdoc:
  packagesToScan: tech.zerofiltre.blog
  pathsToMatch: /**

zerofiltre:
  infra:
    entrypoints:
      rest:
        allowed-origins-pattern: "blog.zerofiltre.tech"